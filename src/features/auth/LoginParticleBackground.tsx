import { GredaParticleBackground } from "@/components/GredaParticleBackground";

/**
 * Fondo Canvas 2D interactivo para la experiencia de Login,
 * desacoplado del ciclo de renderizado de React.
 */
export function LoginParticleBackground() {
  return <GredaParticleBackground variant="login" />;
}

